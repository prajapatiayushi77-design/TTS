[build]
builder = "dockerfile"
dockerfile = "Dockerfile"

[deploy]
startCommand = "sh start.sh"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 5

[env]
PYTHONUNBUFFERED = "1"
DEBUG = "False"
