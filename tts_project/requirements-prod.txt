# Production Requirements for Deployment
# Install all optional dependencies for cloud deployment

# Core dependencies (required)
pyttsx3>=2.90
pydub>=0.25.1
tqdm>=4.65.0
Flask>=2.3.0
Werkzeug>=2.3.0

# Cloud TTS backends (for Vercel & serverless)
edge-tts>=6.1.0      # Cloud-based neural voices (Vercel-friendly)
gTTS>=2.5.0          # Google Text-to-Speech

# Optional but recommended for production
python-dotenv>=1.0.0 # Environment variable management
gunicorn>=21.0.0     # Production WSGI server (for Railway/Render)
psycopg2-binary>=2.9.0 # PostgreSQL support (if using Railway DB)
